#include "main.h"
/**
 * _abs - checks for the sign of integers.
 * @c: checks the passed argument
 * Return: returns 1 if character is positive or negative
 *  otherwise returns 0
 */

int _abs(int c)
{
	if (c < 0)
	{
		c = c * (-1);
	}
		return (c);

}
