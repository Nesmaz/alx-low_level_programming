#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first parameter
 * @b: second parameter
 *Return: (result)
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
