#include <stdio.h>

/**
 * main - Entry point
 * Description : print digit numbers of base 10 from 0 with printf function
 * Return: 0 if successful, non-zero otherwise
 */

int main(void)
{	int i;
	for (i = 0; i < 10; i++)
	{
		printf("%i", i);
	}
	printf("\n");
	return (0);
}
